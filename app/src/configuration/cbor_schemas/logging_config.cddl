SensorLoggingConfig = [
  is_enabled: bool,
  log_raw_value: bool,
  log_only_new_data: bool
]

SensorLoggingConfigMap = { 0*24(uint => SensorLoggingConfig) }

LoggingConfig = [
  logging_interval_ms: uint,
  max_log_size_mb: uint,
  sensor_configurations: SensorLoggingConfigMap
]
